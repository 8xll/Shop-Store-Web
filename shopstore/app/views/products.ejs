<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products | Gaming-Gear Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
        crossorigin="anonymous"></script>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Mitr&display=swap');

        body {
            font-family: 'Mitr', sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
        }

        .content {
            flex: 1;
        }

        footer {
            flex-shrink: 0;
        }
    </style>

</head>

<body>

    <header>
        <%- include('navbar') %>
    </header>


    <div class="content">
        <div class="container mt-5">
            <form method="get" action="/products/searching" class="d-flex">
                <input name="name" value="" class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">ค้นหา</button>
            </form>
        </div>

        <div class="container mt-5">
            <p class="fs-4">รายการสินค้า</p>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-5">
                <% items.forEach(function(item, index) { %>
                    <div class="col">
                        <div class="card h-100">
                            <img src="<%= item.image %>" class="card-img-top" alt="Product Image">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title"><%= item.name %></h5>
                                <p class="card-text flex-grow-1"><%= item.detail %></p>
                                <p class="card-text">เหลือ <%= item.stock %> ชิ้น</p>
                                <p class="card-text">ราคา <%= item.price %></p>
                                <a href="/login" class="btn btn-primary mt-auto">เข้าสู่ระบบเพื่อซื้อ</a>
                            </div>
                        </div>
                    </div>
                    
                    <% }); %>
            </div>
        </div>
    </div>

    <%- include('footer') %>

</body>

</html>